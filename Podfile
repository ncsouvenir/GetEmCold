# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TinyApp-GetEmCold!' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TinyApp-GetEmCold!
	pod 'Firebase/Core'
	pod 'Firebase/Auth’
	pod 'Firebase/Database’
	pod 'Firebase/Storage'
	pod 'Alamofire'


  target 'TinyApp-GetEmCold!Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TinyApp-GetEmCold!UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
